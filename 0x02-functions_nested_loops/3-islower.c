#include "main.h"
/**
 * main - Entry point
 * Description: 'Print c lowercase'
 * Return: Always 0 (success)
 */
int _islower(int c)
{
	if (c = 'a'&& c; c <= 'z')
	{
		return (1):
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
